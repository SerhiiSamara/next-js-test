import styles from "../styles/404.module.scss";

const ERROR = () => {
  return (
    <div className={styles.wrapp}>
      <h1 className={styles.title}>MY PAGE HAVE GOT ERROR</h1>
    </div>
  );
};

export default ERROR;
